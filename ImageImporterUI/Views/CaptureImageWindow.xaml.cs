using System.Windows;

namespace ImageImporterUI.Views;

public partial class CaptureImageWindow : Window
{
    public CaptureImageWindow()
    {
        InitializeComponent();


        Call an initialize command on the view model, when shown
        Call a destroy command on the view model, when closing

    https://www.nickdarnell.com/wpf-viewmodel-attachable-behavior/
    }
}
